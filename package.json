{
  "name": "x402-human-proof",
  "private": true,
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/server --workspace=packages/client",
    "dev": "npm run dev --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "viem": "2.47.10"
  }
}
